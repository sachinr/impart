!!!
%html
  %head
    %meta{content: "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
  %body
    %h1
      ="#{@comment.user.name} commented on a post"
    %p
      ="#{@comment.user.name} posted the following comment on '#{@comment.commentable.title}':"
      %br/
    %p
      ="#{@comment.content}"

    %p
      = link_to 'View the comment', post_url(@comment.commentable)

    %p
      Delivered by a Robot
